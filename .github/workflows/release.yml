name: Release

on:
  push:
    branches: [main]
    paths: [manifest.json]
  workflow_dispatch:
    inputs:
      tag_name:
        description: "Existing release tag for upload-only (e.g., v1.0.0)"
        required: false
        type: string
      upload_only:
        description: "Upload an existing GitHub Release package to Chrome Web Store"
        required: false
        default: false
        type: boolean
      publish_only:
        description: "Submit the package already uploaded to Chrome Web Store"
        required: false
        default: false
        type: boolean

jobs:
  release:
    name: Release
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: chrome-web-store
    permissions:
      contents: write
      id-token: write

    steps:
      - name: Checkout repository
        if: github.event_name == 'push'
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Deno
        if: github.event_name == 'push'
        uses: denoland/setup-deno@v2
        with:
          deno-version: "2.9.5"

      - name: Cache Deno dependencies
        if: github.event_name == 'push'
        uses: actions/cache@v6
        with:
          path: |
            ~/.cache/deno
            ~/.deno
            node_modules
          key: ${{ runner.os }}-deno-${{ hashFiles('deno.json', 'deno.lock') }}
          restore-keys: |
            ${{ runner.os }}-deno-

      - name: Install dependencies
        if: github.event_name == 'push'
        run: deno install

      - name: Read release version
        id: version
        if: github.event_name == 'push'
        run: echo "tag_name=v$(deno eval 'console.log(JSON.parse(Deno.readTextFileSync("manifest.json")).version)')" >> "$GITHUB_OUTPUT"

      - name: Verify release metadata
        if: github.event_name == 'push'
        env:
          RELEASE_REF: ${{ steps.version.outputs.tag_name }}
        run: deno task release:verify "$RELEASE_REF"

      - name: Ensure release tag is new
        if: github.event_name == 'push'
        env:
          RELEASE_REF: ${{ steps.version.outputs.tag_name }}
        run: |
          if git rev-parse --quiet --verify "refs/tags/$RELEASE_REF"; then
            echo "Release tag already exists: $RELEASE_REF" >&2
            exit 1
          fi

      - name: Run linter
        if: github.event_name == 'push'
        run: deno task lint

      - name: Check code formatting
        if: github.event_name == 'push'
        run: deno fmt --check

      - name: Run unit tests
        if: github.event_name == 'push'
        run: deno task test

      - name: Build extension for release
        if: github.event_name == 'push'
        run: deno task build

      - name: Create extension package
        if: github.event_name == 'push'
        env:
          RELEASE_REF: ${{ steps.version.outputs.tag_name }}
        run: (cd dist/release && zip -r "../../markdown-viewer-$RELEASE_REF.zip" .)

      - name: Create GitHub Release
        if: github.event_name == 'push'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.version.outputs.tag_name }}
          target_commitish: ${{ github.sha }}
          name: "Markdown Viewer ${{ steps.version.outputs.tag_name }}"
          body_path: CHANGELOG.md
          files: markdown-viewer-${{ steps.version.outputs.tag_name }}.zip
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ github.token }}

      - name: Download GitHub Release package
        if: github.event_name == 'workflow_dispatch' && inputs.upload_only == true
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_REF: ${{ inputs.tag_name }}
        run: gh release download "$RELEASE_REF" --pattern "markdown-viewer-$RELEASE_REF.zip"

      - name: Authenticate to Google Cloud
        id: auth
        if: github.event_name == 'push' || inputs.upload_only == true || inputs.publish_only == true
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}
          token_format: access_token
          access_token_scopes: https://www.googleapis.com/auth/chromewebstore
          create_credentials_file: false
          export_environment_variables: false

      - name: Upload package to Chrome Web Store
        if: github.event_name == 'push' || inputs.upload_only == true
        env:
          CWS_ACCESS_TOKEN: ${{ steps.auth.outputs.access_token }}
          CWS_PUBLISHER_ID: ${{ vars.CWS_PUBLISHER_ID }}
          CWS_EXTENSION_ID: ${{ vars.CWS_EXTENSION_ID }}
          RELEASE_REF: ${{ github.event_name == 'push' && steps.version.outputs.tag_name || inputs.tag_name }}
        run: |
          curl --fail-with-body --silent --show-error \
            --request POST \
            --header "Authorization: Bearer $CWS_ACCESS_TOKEN" \
            --header "Content-Type: application/zip" \
            --data-binary "@markdown-viewer-$RELEASE_REF.zip" \
            "https://chromewebstore.googleapis.com/upload/v2/publishers/$CWS_PUBLISHER_ID/items/$CWS_EXTENSION_ID:upload"

      - name: Submit Chrome Web Store review
        if: github.event_name == 'workflow_dispatch' && inputs.publish_only == true && inputs.upload_only != true
        env:
          CWS_ACCESS_TOKEN: ${{ steps.auth.outputs.access_token }}
          CWS_PUBLISHER_ID: ${{ vars.CWS_PUBLISHER_ID }}
          CWS_EXTENSION_ID: ${{ vars.CWS_EXTENSION_ID }}
        run: |
          curl --fail-with-body --silent --show-error \
            --request POST \
            --header "Authorization: Bearer $CWS_ACCESS_TOKEN" \
            --header "Content-Type: application/json" \
            --data '{"publishType":"DEFAULT_PUBLISH","deployInfos":[{"deployPercentage":100}],"skipReview":false,"blockOnWarnings":true}' \
            "https://chromewebstore.googleapis.com/v2/publishers/$CWS_PUBLISHER_ID/items/$CWS_EXTENSION_ID:publish"
